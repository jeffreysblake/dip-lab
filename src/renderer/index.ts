// This file is loaded by the renderer process and can use Node.js APIs.
// It's a good place to put initialization logic that needs access to Node.js.

console.log('Renderer process started');

// You can also require other modules here if needed
// const fs = require('fs');

// This file is loaded by the renderer process and can use Node.js APIs.
// It's a good place to put initialization logic that needs access to Node.js.

console.log('Renderer process started');

// You can also require other modules here if needed
// const fs = require('fs');